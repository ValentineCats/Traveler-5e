using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SkillWrapperOld : MonoBehaviour
{
    /*List<Skill> skills;

    void Start()
    {
        
    }


    public void RankSkill(string s, int maxranks = -1)
    {
        for(int i = 0; i < skills.Count; i++)
        {
            if(s == skills[i].GetName())
            {
                if(maxranks < 0 || skills[i].GetRanks() < maxranks)
                {
                    skills[i].AddRanks(1);
                }
                break;
            }
        }
    }

    public void RankSubSkill(string s, string ss, int maxranks = -1)
    {
        for (int i = 0; i < skills.Count; i++)
        {
            if (s == skills[i].GetName())
            {
                if (maxranks < 0 || skills[i].GetRanks() < maxranks)
                {
                    skills[i].AddRanks(1);
                }
                break;
            }
        }
    }*/



}
